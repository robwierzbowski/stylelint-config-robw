// I'm using the TS parser in my eslint config for consistency across repos. The
// parser requires a TS config, and a TS config requires at least one TypeScript
// file in the repo. In the future I'm sure I'll add some types to this repo and
// be able to remove this shim file.
